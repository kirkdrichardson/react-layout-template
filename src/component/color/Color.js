// @flow strict

import * as React from 'react';

type Props = {||};

class Color extends React.Component<Props> {
    render() {
        return (
            <div>
                Color
            </div>
        );
    }
}

export default Color;