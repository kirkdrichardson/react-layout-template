// @flow strict

import * as React from 'react';

type Props = {||};

class Shapes extends React.Component<Props> {
    render() {
        return (
            <div>
                Shapes
            </div>
        );
    }
}

export default Shapes;